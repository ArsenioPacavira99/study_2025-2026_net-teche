
**Рис. 1.3. Отображение содержимого кэша сопоставителя DNS-клиента.**

Данная команда `ipconfig /displaydns` отображает содержимое кэша DNS-клиента, включающее как записи, предварительно загруженные из локального файла Hosts, так и все недавно полученные записи ресурсов для запросов имен, разрешенных компьютером.

**1.2.3. Инициализация динамической регистрации DNS**

Была выполнена команда `ipconfig /registerdns`, которая инициализирует динамическую регистрацию вручную для DNS-имен и IP-адресов, настроенных на компьютере.

**Рис. 1.4. Инициализация динамической регистрации вручную для DNS-имен и IP-адресов.**

**1.2.4. Отображение идентификатора класса DHCP**

Была выполнена команда `ipconfig /showclassid Wi-Fi`, которая отображает идентификатор класса DHCP для указанного адаптера.

**Рис. 1.5. Отображение идентификатора класса DHCP для указанного адаптера.**

#### 1.3. Определение MAC-адресов сетевых интерфейсов

С помощью команды `GETMAC` определим MAC-адреса сетевых интерфейсов на нашем компьютере:

**Рис. 1.6. Определение MAC-адреса сетевых интерфейсов на нашем компьютере.**

Были получены MAC-адреса для всех сетевых адаптеров, включая физические и виртуальные интерфейсы.

---

### Часть 2: Анализ ARP и ICMP протоколов

#### 2.1. Установка Wireshark

**Рис. 2.1. Установка на нашем устройстве Wireshark.**

Wireshark был успешно установлен на устройстве для последующего анализа сетевого трафика.

#### 2.2. Запуск Wireshark и выбор интерфейса

Запустим Wireshark. Выберем активный на нашем устройстве сетевой интерфейс и убедимся, что начался процесс захвата трафика:

**Рис. 2.2. Запуск Wireshark. Выбор активного сетевого интерфейса.**

Для захвата трафика был выбран беспроводной интерфейс Wi-Fi с IP-адресом 100.65.18.114.

#### 2.3. Определение IP-адреса и шлюза по умолчанию

На нашем устройстве в консоли определим с помощью команды `ipconfig` IP-адрес устройства и шлюз по умолчанию:

**Рис. 2.3. Определение IP-адреса устройства и шлюза по умолчанию.**

- **IP-адрес устройства:** 100.65.18.114
- **Шлюз по умолчанию:** 100.65.18.1

#### 2.4. Пинг шлюза по умолчанию

На нашем устройстве в консоли с помощью команды `ping 192.168.160.1` пропигуем шлюз по умолчанию:

**Рис. 2.4. Пинг шлюза по умолчанию.**

Было отправлено 4 эхо-запроса и получено 4 эхо-ответа, что свидетельствует о доступности шлюза.

#### 2.5. Применение фильтра в Wireshark

В Wireshark остановим захват трафика. В строке фильтра пропишем фильтр `arp or icmp` и убедимся, что в списке пакетов отобразились только пакеты ARP или ICMP, в частности пакеты, которые были сгенерированы с помощью команды ping, отправленной с нашего устройства на шлюз по умолчанию:

**Рис. 2.5. Остановка захвата трафика. Фильтр arp or icmp.**

Фильтр позволил выделить только интересующие нас протоколы для дальнейшего анализа.

#### 2.6. Изучение эхо-запроса и эхо-ответа ICMP

**Эхо-запрос ICMP:**

На панели списка пакетов выберем первый указанный кадр ICMP — эхо-запрос:

**Рис. 2.6. Кадр ICMP — эхо-запрос.**

- **MAC-адрес источника:** 70:18:a7:60:9c:fb
- **Тип адреса:** Globally unique address, individual address
- **Протокол:** ICMP (Internet Control Message Protocol)
- **Тип:** 8 (Echo request)
- **Данные:** Содержит информацию о времени отправки и последовательности

**Эхо-ответ ICMP:**

На панели списка пакетов выберем второй указанный кадр ICMP — эхо-ответ:

**Рис. 2.7. Кадр ICMP — эхо-ответ.**

- **MAC-адрес источника:** d4:e9:8a:ea:63:ac
- **Тип адреса:** Globally unique address, individual address
- **Протокол:** ICMP
- **Тип:** 0 (Echo reply)
- **Данные:** Эхо данных запроса, подтверждающие получение

#### 2.7. Изучение кадров ARP

Изучим кадры данных протокола ARP и данные в полях заголовка Ethernet II:

**Рис. 2.8. Изучение кадров данных протокола ARP и данных в полях заголовка Ethernet II.**

**Анализ ARP-пакета:**
- **Hardware type:** Ethernet (1)
- **Protocol type:** IPv4 (0x0800)
- **Hardware size:** 6
- **Protocol size:** 4
- **Opcode:** request/reply
- **Sender MAC address:** MAC-адрес отправителя
- **Sender IP address:** IP-адрес отправителя
- **Target MAC address:** MAC-адрес получателя
- **Target IP address:** IP-адрес получателя

ARP-протокол используется для определения MAC-адреса по известному IP-адресу в локальной сети.

#### 2.8. Пинг по имени домена

Начнём новый процесс захвата трафика в Wireshark. На нашем устройстве в консоли пропигуем по имени адрес `ping www.yandex.ru`:

